'use strict';

/**
 * @ngdoc function
 * @name whiskeyApp.controller:DetailsCtrl
 * @description
 * # DetailsCtrl
 * Controller of the whiskeyApp
 */
angular.module('whiskeyApp')
  .controller('DetailsCtrl', function($scope, $routeParams, Whiskey) {
    // TODO Populate `$scope.whiskey` with whiskey data
    // The id is in `$routeParams.id`
    // Use `Whiskey.findOne` and add `include` to the `filter` property
    // to fetch the list of reviews too.
  });
